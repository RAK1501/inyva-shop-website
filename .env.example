# Web3Forms access key, from web3forms.com.
# Public by design: it can only deliver to the address that registered it,
# and it is inlined into the client bundle. Set it on every environment.
NEXT_PUBLIC_WEB3FORMS_KEY=
